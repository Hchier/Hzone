server:
  port: 8021


spring:
  application:
    name: service-topic

  redis:
    host: 127.0.0.1
    port: 6379
    password: pyh903903

  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848

  datasource:
    username: root
    password: pyh903903
    url: jdbc:mysql://localhost:3306/hzone?useSSL=true&useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai
    driver-class-name: com.mysql.cj.jdbc.Driver
    type: com.alibaba.druid.pool.DruidDataSource


properties:
  # 话题阅读总榜
  zsetForTopicTotalReadNumChart: "topicTotalReadNumChart"

  # 话题阅读周榜
  zsetForTopicWeekReadNumChart: "topicWeekReadNumChart"

  # 话题阅读日榜
  zsetForTopicDayReadNumChart: "topicDayReadNumChart"

mybatis:
  #mapper-locations: classpath:hchier\springboot\mapper\*.xml
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl  #输出sql到控制台
    map-underscore-to-camel-case: true