server:
  port: 8071

spring:
  application:
    name: service-ws

  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848

  redis:
    host: 127.0.0.1
    port: 6379
    password: pyh903903

properties:
  # redis中存放token的hash
  hashForToken: tokens